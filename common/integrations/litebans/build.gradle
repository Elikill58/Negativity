sourceSets {
    main {
        java.srcDirs = ['src']
        resources.srcDirs = ['resources']
    }
}

dependencies {
    compileOnly 'com.gitlab.ruany:LiteBansAPI:0.3.4'
    api(project(':common'))
}
